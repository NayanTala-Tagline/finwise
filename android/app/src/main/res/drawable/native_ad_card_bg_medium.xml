<?xml version="1.0" encoding="utf-8"?>
<!-- Medium native ad card — uses the app scaffold background so the card blends
     into the screen. No elevation (keeps the AdChoices icon visible). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/native_ad_surface" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/native_ad_stroke" />
</shape>
